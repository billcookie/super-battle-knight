<head>
  <link href="https://api.mapbox.com/mapbox-gl-js/v1.11.1/mapbox-gl.css" rel="stylesheet">
</head>
<div class="container text-center">
  <div class="flex-card d-flex flex-column justify-items-center">
    <h1><%= @battle.title%></h1>
    <h3><%= @battle.fight_date %></h3>
    <p><%= @battle.location %></p>
    <div class="container form-submission" data-controller="get-all-addresses">
      <form>
        <input class="form-control" type="hidden" id="custId" name="custId" value=<%=@battle.location%> data-get-all-addresses-target="address">
        <input class="map-form-btn" type="submit" value="Show location" class="btn btn-primary" data-action="click->get-all-addresses#geocodeSubmit">
      </form>
      <div id="map" style="height: 300px; width: 600px">
        <!-- Display the map in this div -->
      </div>
    </div>
  </div>
</div>
</div>
</div>
<div class="text-center">
  <h2>Participants</h2>
</div>
<div class="container">
  <div class="cards">
    <% @battle.obligations.each do |obligation| %>
      <div class="card-category zoom" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= obligation.character.photo.attached? ? cl_image_path(obligation.character.photo.key,crop: :thumb,gravity: :face) : 'http://source.unsplash.com/featured/?warrior' %>)">
        <%= obligation.character.name %>
      </div>
    <% end %>
  </div>
</div>
